# Ralph Progress Log
Started: Sun Feb 22 06:31:30 EET 2026
---

## Codebase Patterns
- Target repos are separate: `aptabase-com` (Astro marketing site) vs `aptabase` (main app). Clone repos as needed.
- Astro serves `public/` directory at URL root — static files like `llms.txt` go in `public/`
- SDK framework list is defined in `aptabase-com/src/lib/frameworks.ts` with slugs, URLs, and code examples
- aptabase-com has no lint/test setup — only `astro build` for validation
- There are 16 SDKs across 12 repos (aptabase-js is a monorepo with 4 packages: web, react, angular, browser)
- NativeScript SDK is community-maintained at `nstudio/nativescript-plugins`, not under `aptabase/` org
- Docs pages are at `src/pages/docs/` as `.md` files (currently: build-modes.md, apple-app-privacy.md)
- Footer component is at `aptabase-com/src/components/Footer.astro`
- robots.txt is at `aptabase-com/public/robots.txt`
- Footer links are data-driven arrays in Astro frontmatter (`product`, `resources`, `blog`, `company`) — append to add new links
- SDK repos are cloned to `/Users/bogdan.ivanov/repos/personal/aptabase/` — each needs its own feature branch
- For SDK llms.txt: read README via `gh api repos/aptabase/REPO/readme`, then check source code for init options and config types
- The template is a starting point — adapt structure for SDKs with unique architecture (e.g., Electron main/renderer split, Tauri Rust/JS dual API)

## 2026-02-22 - US-001
- Created `public/llms.txt` in aptabase-com repo with full SDK catalog
- File follows llms.txt spec: H1 heading, blockquote summary, Documentation section, SDKs section (16 SDKs organized by Desktop/Mobile/Web/Games/Other), Optional section
- File size: 3,242 bytes (under 4KB limit)
- Files changed: `aptabase-com/public/llms.txt` (new file)
- Committed on branch `ralph/llms-txt-agent-discovery` in aptabase-com repo
- **Learnings for future iterations:**
  - aptabase-com needs to be cloned separately — it's at `github.com/aptabase/aptabase-com`
  - The llms.txt spec (llmstxt.org) requires: H1 (required), blockquote (optional), body sections, H2-delimited file lists with `[name](url): description` format
  - The "## Optional" H2 section in llms.txt spec indicates content that can be skipped for shorter context
  - SDK READMEs on GitHub are the source of truth for install commands and API details — use `gh api repos/aptabase/REPO/readme` to fetch them
---

## 2026-02-22 - US-002
- Created `public/llms-full.txt` in aptabase-com repo with complete inlined integration guides for all 16 SDKs
- File size: 14,340 bytes (~14KB) — self-contained, no external URL fetching needed
- Covers all 16 SDKs across 12 repos with install, init, and trackEvent code examples
- Tauri section: covers both Rust (EventTracker trait) and JavaScript APIs
- Electron section: documents main/renderer process split with separate import paths
- React section: covers Next.js App Router, Pages Router, Remix, CRA, and Vite
- aptabase-js: covered as 4 separate sections (Web, React, Angular, Browser Extensions)
- Files changed: `aptabase-com/public/llms-full.txt` (new file)
- astro build passes successfully
- **Learnings for future iterations:**
  - aptabase-js is a Turborepo monorepo — main README just lists sub-packages, real content is in `packages/*/README.md`
  - To fetch sub-package READMEs: `gh api repos/aptabase/aptabase-js/contents/packages` then fetch each `README.md`
  - Python SDK is fully async (httpx + asyncio, Python 3.11+) — different pattern from other SDKs
  - C++ SDK has pluggable HTTP backends (cpp-httplib or Boost) and requires explicit session management (StartSession/EndSession)
  - aptabase-com needs `npm install` before `npx astro build` if node_modules is missing
---

## 2026-02-22 - US-003
- Updated `public/robots.txt` in aptabase-com repo to reference llms.txt
- Added comment line: `# AI agent documentation: https://aptabase.com/llms.txt`
- All existing directives preserved unchanged (User-agent, Allow, Sitemap)
- Files changed: `aptabase-com/public/robots.txt` (modified)
- astro build passes successfully
- **Learnings for future iterations:**
  - robots.txt changes are trivial — just add/modify lines in `aptabase-com/public/robots.txt`
  - The llms.txt reference in robots.txt is a comment (# prefix), not a directive — this is the convention from the llms.txt spec
---

## 2026-02-22 - US-004
- Added `llms.txt` link to the aptabase.com footer in the Resources section
- Link renders as `<a href="/llms.txt">llms.txt</a>` with existing footer styling
- Verified via `astro build` — link appears in all built pages
- Files changed: `aptabase-com/src/components/Footer.astro` (modified)
- **Learnings for future iterations:**
  - Footer links are data-driven arrays in the Astro frontmatter — just append to the appropriate array (`product`, `resources`, `blog`, `company`)
  - Browser MCP may not always be available — verify via build output HTML as fallback
---

## 2026-02-22 - US-005
- Created `tasks/templates/sdk-llms-txt-template.md` with standardized template for SDK llms.txt files
- Template sections: Overview, Installation, Initialization, Track Events, Track Events with Properties, Configuration, Platform Notes, Cross-Discovery
- Uses `{{PLACEHOLDER}}` variables for SDK-specific values (SDK_DISPLAY_NAME, PACKAGE_NAME, INSTALL_COMMAND, REPO_URL, PLATFORM_NAME, LANG, etc.)
- Follows llms.txt spec format: H1 heading with SDK name, blockquote summary, H2-delimited sections
- Includes cross-discovery link back to `https://aptabase.com/llms.txt`
- Files changed: `tasks/templates/sdk-llms-txt-template.md` (new file)
- **Learnings for future iterations:**
  - Template uses double-brace `{{VAR}}` placeholders — when filling in, replace entire placeholder including braces
  - The `{{LANG}}` placeholder sets the code fence language (e.g., `js`, `swift`, `kotlin`, `dart`, `csharp`, `python`, `cpp`, `rust`)
  - Configuration and Platform Notes sections are free-form text — adapt to each SDK's specific needs
---

## 2026-02-22 - US-006
- Created `llms.txt` at root of `aptabase-electron` repo
- Documents main vs renderer process split with separate import paths
- Covers: initialization (main process), trackEvent (both processes), properties, configuration (self-hosted host option)
- Platform notes: Electron >= 3.x, must init before app.whenReady(), event buffering, IPC relay
- File size: 3,014 bytes
- Files changed: `aptabase-electron/llms.txt` (new file)
- Committed on branch `ralph/llms-txt-agent-discovery` in aptabase-electron repo
- **Learnings for future iterations:**
  - SDK repos need their own feature branch created (they're separate git repos, not the same as aptabase or aptabase-com)
  - `initialize` function in Electron SDK has an `AptabaseOptions` type with optional `host` for self-hosted instances
  - Electron SDK uses IPC and a custom `aptabase-ipc` protocol to relay events from renderer to main process
  - Sessions auto-rotate after 1 hour of inactivity (SESSION_TIMEOUT = 3600s)
  - The template is a good starting point but Electron needs a custom "Main vs Renderer Process" section not in the template
---

## 2026-02-22 - US-008
- Created `llms.txt` at root of `aptabase-swift` repo
- Documents SPM install (with both Package.swift and Xcode), CocoaPods install
- Covers: initialization (SwiftUI and UIKit), trackEvent, trackEvent with properties, InitOptions configuration
- Documents all supported platforms: iOS, macOS, watchOS (7.0+), tvOS, visionOS
- Configuration section covers `host` (self-hosted), `flushInterval`, and `trackingMode` (.readFromEnvironment, .asDebug, .asRelease)
- Includes macOS App Sandbox note and Apple App Privacy link
- File size: 3,959 bytes
- Files changed: `aptabase-swift/llms.txt` (new file)
- Committed on branch `ralph/llms-txt-agent-discovery` in aptabase-swift repo
- **Learnings for future iterations:**
  - Swift SDK supports `Value` protocol for property values — accepts String, Int, Double, Float, and Bool
  - `InitOptions` has three fields: `host` (String?), `flushInterval` (NSNumber? → Double?), and `trackingMode` (TrackingMode)
  - TrackingMode is a custom class (not an enum) with static values: `.asDebug`, `.asRelease`, `.readFromEnvironment`
  - The SDK uses NotificationCenter to observe app lifecycle (foreground/background) for session management
  - visionOS is supported alongside iOS, macOS, watchOS, tvOS
  - Both SwiftUI and UIKit init patterns are worth documenting — many agents may use either
---

## 2026-02-22 - US-007
- Created `llms.txt` at root of `tauri-plugin-aptabase` repo
- Documents dual API: Rust (`EventTracker` trait on App/AppHandle/Window) and JavaScript (`trackEvent` from `@aptabase/tauri`)
- Covers: plugin registration via `Builder::new().build()`, ACL permission (`aptabase:allow-track-event`), Rust tracking with `serde_json::json!()`, JS tracking, configuration (`InitOptions` with `host` and `flush_interval`)
- Platform notes: Tauri v2, MSRV 1.70, panic hook support
- File size: 4,688 bytes
- Files changed: `tauri-plugin-aptabase/llms.txt` (new file)
- Committed on branch `ralph/llms-txt-agent-discovery` in tauri-plugin-aptabase repo
- **Learnings for future iterations:**
  - Tauri v2 uses a `Builder` pattern: `tauri_plugin_aptabase::Builder::new(key).build()` — the README also shows a legacy `init()` function but `Builder` is the canonical API
  - ACL permissions for Tauri v2 plugins go in `src-tauri/capabilities/*.json` — the permission identifier is `aptabase:allow-track-event`
  - `InitOptions` has two fields: `host` (for self-hosted) and `flush_interval` (defaults to 60s release / 2s debug)
  - Rust `track_event` takes `Option<serde_json::Value>` for properties, not a HashMap
  - `flush_events_blocking()` is used at app exit to ensure pending events are sent
---

## 2026-02-22 - US-009
- Created `llms.txt` at root of `aptabase-kotlin` repo
- Documents JitPack repository setup in `settings.gradle.kts` and Gradle dependency
- Covers: initialization in Application class, trackEvent, trackEvent with properties, self-hosted InitOptions
- Configuration section covers `InitOptions(host)`, session timeout, background thread pool
- Platform notes: Android-only, requires Application subclass, uses HttpURLConnection, auto debug detection
- File size: 2,875 bytes
- Files changed: `aptabase-kotlin/llms.txt` (new file)
- Committed on branch `ralph/llms-txt-agent-discovery` in aptabase-kotlin repo
- **Learnings for future iterations:**
  - Kotlin SDK uses `Aptabase.instance` singleton pattern (companion object with `@JvmStatic`)
  - `InitOptions` is a data class with single field: `host: String?` — only needed for self-hosted (`A-SH-*`) app keys
  - Property values support only `String` and `Int` (via `Map<String, Any>`)
  - SDK uses `HttpURLConnection` directly — no OkHttp or Retrofit dependency
  - Debug mode comes from `ApplicationInfo.FLAG_DEBUGGABLE` in `EnvironmentInfo`
  - Background execution uses `Executors.newFixedThreadPool(5)` with shutdown hook
---

## 2026-02-22 - US-010
- Created `llms.txt` at root of `aptabase_flutter` repo
- Documents installation (`flutter pub add aptabase_flutter`) with Android INTERNET permission note
- Covers: async initialization in main.dart, trackEvent, trackEvent with properties, InitOptions configuration
- Configuration section covers `host` (self-hosted), `tickDuration` (flush interval, default 30s), `batchLength` (max 25), `printDebugMessages`
- Platform notes: all 6 platforms (Android, iOS, macOS, Web, Linux, Windows), session timeout, batch queuing, auto debug detection
- File size: 3,464 bytes
- Files changed: `aptabase_flutter/llms.txt` (new file)
- Committed on branch `ralph/llms-txt-agent-discovery` in aptabase_flutter repo
- **Learnings for future iterations:**
  - Flutter SDK uses `Aptabase.instance` singleton pattern (similar to Kotlin SDK)
  - `InitOptions` is a const constructor class with 4 fields: `host`, `tickDuration`, `batchLength`, `printDebugMessages`
  - `batchLength` has a hard max of 25 enforced via assertion
  - Flutter SDK uses `StorageManager` abstraction (SharedPrefs by default) for event persistence — events survive app restarts
  - `WidgetsFlutterBinding.ensureInitialized()` is required before `Aptabase.init` — this is a common Flutter gotcha worth highlighting
  - `AppLifecycleListener` is used for session management (flush on inactive, restart timer on resume)
---

## 2026-02-22 - US-011
- Created `llms.txt` at root of `aptabase-react-native` repo
- Documents npm install and Android INTERNET permission requirement
- Covers: init (direct and AptabaseProvider context), trackEvent, trackEvent with properties, useAptabase hook, dispose
- Configuration section covers `host` (self-hosted), `appVersion` (Expo Go workaround), `flushInterval` (override flush interval)
- Platform notes: iOS/Android, Expo Go app version limitation, session timeout, background flush behavior
- File size: 2,652 bytes
- Files changed: `aptabase-react-native/llms.txt` (new file)
- Committed on branch `ralph/llms-txt-agent-discovery` in aptabase-react-native repo
- **Learnings for future iterations:**
  - React Native SDK exports both direct functions (`init`, `trackEvent`, `dispose`) and React Context (`AptabaseProvider`, `useAptabase`)
  - `AptabaseOptions` has 3 fields: `host` (self-hosted), `appVersion` (override), `flushInterval` (ms)
  - Property values accept `string | number | boolean` — broader than Kotlin (string | int only)
  - `__DEV__` global controls flush interval: 2s dev, 60s prod
  - Expo Go lacks native modules so `appVersion` is unavailable — the `appVersion` init option is a documented workaround
  - `dispose()` stops polling and sets client to undefined — can be called to fully deinitialize the SDK
---

## 2026-02-22 - US-012
- Created `llms.txt` at root of `aptabase-maui` repo
- Documents NuGet install (`dotnet add package Aptabase.Maui`) and .csproj reference
- Covers: `UseAptabase` extension on `MauiAppBuilder`, DI-based `IAptabaseClient` injection, `TrackEvent`, properties, full `AptabaseOptions` config table
- Configuration section covers `Host` (self-hosted), `IsDebugMode`, `EnablePersistence`, `EnableCrashReporting`
- Platform notes: all MAUI platforms (Android, iOS, macOS, Windows), non-blocking TrackEvent, IAsyncDisposable
- File size: 3,207 bytes
- Files changed: `aptabase-maui/llms.txt` (new file)
- Committed on branch `ralph/llms-txt-agent-discovery` in aptabase-maui repo
- **Learnings for future iterations:**
  - MAUI SDK uses DI pattern — `UseAptabase` registers `IAptabaseClient` as singleton, pages inject it via constructor
  - `AptabaseOptions` has 4 fields: `Host`, `IsDebugMode`, `EnablePersistence`, `EnableCrashReporting` (all nullable)
  - `EnablePersistence` switches implementation from `AptabaseClient` to `AptabasePersistentClient` — persists events to disk
  - `EnableCrashReporting` creates an `AptabaseCrashReporter` that logs unhandled exceptions as events with stack frames
  - `IAptabaseClient` implements `IAsyncDisposable` — TrackEvent signature is `Task TrackEvent(string, Dictionary<string, object>?)`
  - Property values support only strings and numbers (via `Dictionary<string, object>`)
  - Pages that use DI injection must be registered in `builder.Services.AddSingleton<PageType>()` — common MAUI pattern worth documenting
---

## 2026-02-22 - US-013
- Created `llms.txt` at root of `aptabase-js` repo documenting all 4 packages
- **@aptabase/web**: init/trackEvent functions, AptabaseOptions config, SPA-focused notes
- **@aptabase/react**: AptabaseProvider/useAptabase pattern, setup examples for Next.js App Router, Pages Router, Remix, CRA/Vite
- **@aptabase/angular**: Both standalone API (provideAptabaseAnalytics) and NgModule (AptabaseAnalyticsModule.forRoot) setup, AptabaseAnalyticsService injection
- **@aptabase/browser**: Background script init, cross-context tracking via chrome.runtime.sendMessage, chrome.storage.local session persistence
- All packages share the same `AptabaseOptions` type: `host`, `apiUrl`, `appVersion`, `isDebug`
- File size: 6,524 bytes
- Files changed: `aptabase-js/llms.txt` (new file)
- Committed on branch `ralph/llms-txt-agent-discovery` in aptabase-js repo
- **Learnings for future iterations:**
  - All 4 JS SDK packages share a common `shared.ts` at `packages/shared.ts` — `AptabaseOptions` type and `sendEvent`/`sendError` functions are defined there
  - `AptabaseOptions` has 4 fields across all packages: `host`, `apiUrl`, `appVersion`, `isDebug`
  - Browser extension SDK must init in background script — events from popup/content scripts relay via `chrome.runtime.sendMessage`
  - Browser SDK persists session ID in `chrome.storage.local` (unlike web/react which use in-memory session)
  - React SDK queues events fired before init completes via `_eventsBeforeInit` map — then flushes on init
  - Angular SDK supports both standalone API (`provideAptabaseAnalytics`) and legacy NgModule (`AptabaseAnalyticsModule.forRoot`)
  - Property values accept `string | number | boolean` across all JS packages
---

## 2026-02-22 - US-014
- Created `llms.txt` at root of `aptabase-python` repo
- Documents fully async API: context manager (`async with`) and manual lifecycle (`start`/`stop`)
- Covers: installation (pip/uv), track/track with properties, full configuration table, error handling
- Configuration section covers all 7 init parameters: `app_key`, `app_version`, `is_debug`, `max_batch_size`, `flush_interval`, `timeout`, `base_url`
- Platform notes: Python 3.11+, httpx, auto-batching, session rotation, region-based endpoints
- File size: 3,307 bytes
- Files changed: `aptabase-python/llms.txt` (new file)
- Committed on branch `ralph/llms-txt-agent-discovery` in aptabase-python repo
- **Learnings for future iterations:**
  - Python SDK is fully async — all tracking is `await client.track()`, not synchronous
  - Constructor parameter is `base_url` (not `host` like other SDKs) — used for self-hosted instances with `A-SH-*` keys
  - SDK uses `httpx.AsyncClient` for HTTP — no sync fallback available
  - Error hierarchy: `AptabaseError` → `ConfigurationError`, `NetworkError`, `ValidationError`
  - `NetworkError` has a `status_code` attribute for HTTP error inspection
  - Session IDs are generated from epoch seconds + random number (not UUID like Event default)
---

## 2026-02-22 - US-015
- Created `llms.txt` at root of `aptabase-unity` repo
- Documents Unity Package Manager install via git URL
- Covers: auto-initialization via `[RuntimeInitializeOnLoadMethod]`, `TrackEvent` static API, properties, `Flush()`, full `AptabaseSettings` config table
- Configuration section covers `AppKey`, `SelfHostURL`, `AppBuildNumber`, `EnableOverride`, `AppVersion`, `FlushInterval`
- Platform notes: all 6 platforms (Android, iOS/iPadOS, macOS, Windows, Linux, WebGL), WebGL single-event dispatch, session timeout, offline resilience
- File size: 3,164 bytes
- Files changed: `aptabase-unity/llms.txt` (new file)
- Committed on branch `ralph/llms-txt-agent-discovery` in aptabase-unity repo
- **Learnings for future iterations:**
  - Unity SDK uses `[RuntimeInitializeOnLoadMethod(BeforeSceneLoad)]` for auto-init — no MonoBehaviour setup needed by the user
  - Configuration is via a ScriptableObject at `Assets/Aptabase/Resources/AptabaseSettings` — not code-based init like other SDKs
  - `EnableOverride` boolean gates `AppVersion` and `FlushInterval` fields — both are ignored unless toggle is on
  - WebGL uses a separate `WebGLDispatcher` that sends events individually (no batching) via `/api/v0/event` (singular)
  - Non-WebGL uses `/api/v0/events` (plural) with max batch size of 25
  - SDK embeds TinyJSON for serialization — no external dependencies
  - `AptabaseService` MonoBehaviour is created internally and marked `DontDestroyOnLoad` for lifecycle hooks (focus/blur)
  - Namespace is `AptabaseSDK`, not `Aptabase` — the static class `Aptabase` is inside the `AptabaseSDK` namespace
---

## 2026-02-22 - US-016
- Created `llms.txt` at root of `aptabase-unreal` repo
- Documents plugin installation (clone into Plugins folder), enabling in Editor, DefaultEngine.ini config
- Documents C++ tracking via `FAnalytics` provider-agnostic API with `RecordEvent` and `FAnalyticsEventAttribute`
- Documents Blueprint tracking via Blueprint Analytics Plugin nodes
- Configuration table covers `AppKey`, `Host` (auto-detected EU/US/DEV/SH), `CustomHost`, `SendInterval`, `DebugSendInterval`
- Platform notes: UE 5.x, batched events, non-blocking, auto session management
- File size: 3,197 bytes
- Files changed: `aptabase-unreal/llms.txt` (new file)
- Committed on branch `ralph/llms-txt-agent-discovery` in aptabase-unreal repo
- **Learnings for future iterations:**
  - Unreal SDK uses UE's built-in `IAnalyticsProvider` interface — it's provider-agnostic, not Aptabase-specific API
  - Configuration is via `UDeveloperSettings` subclass (`UAptabaseSettings`) stored in `Config/DefaultAptabase.ini` at runtime
  - `EAptabaseHost` enum auto-detects from AppKey prefix: `A-EU-*` → EU, `A-US-*` → US, `A-DEV-*` → DEV, `A-SH-*` → SH (self-hosted)
  - `FAnalyticsEventAttribute` is UE's built-in type — it wraps key-value pairs (strings and numbers)
  - The Multicast Analytics Provider plugin lets you run Aptabase alongside other analytics providers
  - Blueprint Analytics Plugin provides provider-agnostic nodes like `Record Event with Attributes`
---

## 2026-02-22 - US-017
- Created `llms.txt` at root of `aptabase-cpp` repo
- Documents CMake integration with both cpp-httplib and Boost.Asio backends
- Covers: initialization with HttpClient + app key + URL + debug flag, session management (StartSession/EndSession), RecordEvent, properties
- Documents all 3 Provider strategies: AutoProvider (immediate), ManualProvider (explicit Flush), WorkerProvider (background thread)
- Configuration section covers runtime setters: SetAppVersion, SetLocale, SetOsName, SetOsVersion, SetDebug, SetLog
- Platform notes: C++17, pluggable HTTP backend, max 25 events/request, EventAttribute values are variant<string, float, double>
- File size: 4,369 bytes
- Files changed: `aptabase-cpp/llms.txt` (new file)
- Committed on branch `ralph/llms-txt-agent-discovery` in aptabase-cpp repo
- **Learnings for future iterations:**
  - C++ SDK has explicit session management (StartSession/EndSession) — unlike other SDKs that auto-manage sessions
  - Three Provider strategies control dispatch: AutoProvider (immediate, default), ManualProvider (explicit Flush), WorkerProvider (background thread)
  - EventAttribute values are `std::variant<std::string, float, double>` — no int or bool support (narrower than most SDKs)
  - No built-in platform detection — OS name, version, locale must be set manually via setter methods
  - The SDK requires users to supply their own HTTP backend dependencies (cpp-httplib or Boost) — CMake flags `CMAKE_APTABASE_USE_HTTPLIB` or `CMAKE_APTABASE_USE_BOOST` control compilation
  - Custom HTTP backends can be created by implementing the `Aptabase::HttpClient` abstract interface
---

## 2026-02-22 - US-018
- Updated README.md in all 12 Aptabase-owned SDK repos to reference llms.txt
- Added line at the bottom of each README: `For AI/LLM integration instructions, see [llms.txt](./llms.txt)`
- NativeScript excluded (community-maintained, different org)
- Repos updated: aptabase-electron, tauri-plugin-aptabase, aptabase-swift, aptabase-kotlin, aptabase_flutter, aptabase-react-native, aptabase-maui, aptabase-js, aptabase-python, aptabase-unity, aptabase-unreal, aptabase-cpp
- Files changed: `README.md` in each of the 12 repos (modified)
- All committed on branch `ralph/llms-txt-agent-discovery` in their respective repos
- **Learnings for future iterations:**
  - All 12 SDK repos were already cloned and on the correct feature branch from previous iterations
  - READMEs have varied structures — some end with "Apple App Store" section, some with code examples, some with License section. Adding the llms.txt reference at the very end works consistently regardless of structure
  - aptabase-kotlin README had no trailing newline — edit tool handled this gracefully
  - aptabase-python README also had no trailing newline at EOF
  - Batch operations across 12 repos can be done efficiently via shell loops for verification and commits
---

## 2026-02-22 - US-019
- Submitted aptabase.com/llms.txt to llms.txt directories and listings
- **llmstxthub.com**: PR submitted via GitHub — https://github.com/thedaviddias/llms-txt-hub/pull/599 (category: data-analytics)
- **llmstxt.site**: Form-based submission documented (https://llmstxt.site/submit) — pending deployment
- **directory.llmstxt.cloud**: Tally form submission documented (https://tally.so/r/wAydjB) — pending deployment
- **llms-text.com**: Investigated — no public submission form found, needs further investigation
- **llmstxt.org**: Does not maintain its own directory — refers to llmstxt.site and directory.llmstxt.cloud
- Created tracking issue with all submission details: https://github.com/aptabase/aptabase-com/issues/11
- Files changed: `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`
- **Learnings for future iterations:**
  - llmstxthub.com accepts submissions via GitHub PRs to `thedaviddias/llms-txt-hub` repo — add entry to `data/websites.json` alphabetically
  - Entry format: `{ name, domain, description, llmsTxtUrl, llmsFullTxtUrl, category, favicon, publishedAt }`
  - Valid categories: ai-ml, developer-tools, data-analytics, integration-automation, infrastructure-cloud, security-identity, personal, agency, e-commerce, education, media, international, other
  - llmstxt.site and directory.llmstxt.cloud use web forms — need browser interaction (can't be automated via CLI)
  - aptabase.com/llms.txt returns 404 until deployed — form-based directories may validate the URL on submission
  - Creating a GitHub issue to track submission status across multiple directories is a good practice for manual follow-up tasks
---

# Ralph Progress Log
Started: Sun Feb 22 06:31:30 EET 2026
---

## Codebase Patterns
- Target repos are separate: `aptabase-com` (Astro marketing site) vs `aptabase` (main app). Clone repos as needed.
- Astro serves `public/` directory at URL root — static files like `llms.txt` go in `public/`
- SDK framework list is defined in `aptabase-com/src/lib/frameworks.ts` with slugs, URLs, and code examples
- aptabase-com has no lint/test setup — only `astro build` for validation
- There are 16 SDKs across 12 repos (aptabase-js is a monorepo with 4 packages: web, react, angular, browser)
- NativeScript SDK is community-maintained at `nstudio/nativescript-plugins`, not under `aptabase/` org
- Docs pages are at `src/pages/docs/` as `.md` files (currently: build-modes.md, apple-app-privacy.md)
- Footer component is at `aptabase-com/src/components/Footer.astro`
- robots.txt is at `aptabase-com/public/robots.txt`
- Footer links are data-driven arrays in Astro frontmatter (`product`, `resources`, `blog`, `company`) — append to add new links
- SDK repos are cloned to `/Users/bogdan.ivanov/repos/personal/aptabase/` — each needs its own feature branch
- For SDK llms.txt: read README via `gh api repos/aptabase/REPO/readme`, then check source code for init options and config types
- The template is a starting point — adapt structure for SDKs with unique architecture (e.g., Electron main/renderer split, Tauri Rust/JS dual API)

## 2026-02-22 - US-001
- Created `public/llms.txt` in aptabase-com repo with full SDK catalog
- File follows llms.txt spec: H1 heading, blockquote summary, Documentation section, SDKs section (16 SDKs organized by Desktop/Mobile/Web/Games/Other), Optional section
- File size: 3,242 bytes (under 4KB limit)
- Files changed: `aptabase-com/public/llms.txt` (new file)
- Committed on branch `ralph/llms-txt-agent-discovery` in aptabase-com repo
- **Learnings for future iterations:**
  - aptabase-com needs to be cloned separately — it's at `github.com/aptabase/aptabase-com`
  - The llms.txt spec (llmstxt.org) requires: H1 (required), blockquote (optional), body sections, H2-delimited file lists with `[name](url): description` format
  - The "## Optional" H2 section in llms.txt spec indicates content that can be skipped for shorter context
  - SDK READMEs on GitHub are the source of truth for install commands and API details — use `gh api repos/aptabase/REPO/readme` to fetch them
---

## 2026-02-22 - US-002
- Created `public/llms-full.txt` in aptabase-com repo with complete inlined integration guides for all 16 SDKs
- File size: 14,340 bytes (~14KB) — self-contained, no external URL fetching needed
- Covers all 16 SDKs across 12 repos with install, init, and trackEvent code examples
- Tauri section: covers both Rust (EventTracker trait) and JavaScript APIs
- Electron section: documents main/renderer process split with separate import paths
- React section: covers Next.js App Router, Pages Router, Remix, CRA, and Vite
- aptabase-js: covered as 4 separate sections (Web, React, Angular, Browser Extensions)
- Files changed: `aptabase-com/public/llms-full.txt` (new file)
- astro build passes successfully
- **Learnings for future iterations:**
  - aptabase-js is a Turborepo monorepo — main README just lists sub-packages, real content is in `packages/*/README.md`
  - To fetch sub-package READMEs: `gh api repos/aptabase/aptabase-js/contents/packages` then fetch each `README.md`
  - Python SDK is fully async (httpx + asyncio, Python 3.11+) — different pattern from other SDKs
  - C++ SDK has pluggable HTTP backends (cpp-httplib or Boost) and requires explicit session management (StartSession/EndSession)
  - aptabase-com needs `npm install` before `npx astro build` if node_modules is missing
---

## 2026-02-22 - US-003
- Updated `public/robots.txt` in aptabase-com repo to reference llms.txt
- Added comment line: `# AI agent documentation: https://aptabase.com/llms.txt`
- All existing directives preserved unchanged (User-agent, Allow, Sitemap)
- Files changed: `aptabase-com/public/robots.txt` (modified)
- astro build passes successfully
- **Learnings for future iterations:**
  - robots.txt changes are trivial — just add/modify lines in `aptabase-com/public/robots.txt`
  - The llms.txt reference in robots.txt is a comment (# prefix), not a directive — this is the convention from the llms.txt spec
---

## 2026-02-22 - US-004
- Added `llms.txt` link to the aptabase.com footer in the Resources section
- Link renders as `<a href="/llms.txt">llms.txt</a>` with existing footer styling
- Verified via `astro build` — link appears in all built pages
- Files changed: `aptabase-com/src/components/Footer.astro` (modified)
- **Learnings for future iterations:**
  - Footer links are data-driven arrays in the Astro frontmatter — just append to the appropriate array (`product`, `resources`, `blog`, `company`)
  - Browser MCP may not always be available — verify via build output HTML as fallback
---

## 2026-02-22 - US-005
- Created `tasks/templates/sdk-llms-txt-template.md` with standardized template for SDK llms.txt files
- Template sections: Overview, Installation, Initialization, Track Events, Track Events with Properties, Configuration, Platform Notes, Cross-Discovery
- Uses `{{PLACEHOLDER}}` variables for SDK-specific values (SDK_DISPLAY_NAME, PACKAGE_NAME, INSTALL_COMMAND, REPO_URL, PLATFORM_NAME, LANG, etc.)
- Follows llms.txt spec format: H1 heading with SDK name, blockquote summary, H2-delimited sections
- Includes cross-discovery link back to `https://aptabase.com/llms.txt`
- Files changed: `tasks/templates/sdk-llms-txt-template.md` (new file)
- **Learnings for future iterations:**
  - Template uses double-brace `{{VAR}}` placeholders — when filling in, replace entire placeholder including braces
  - The `{{LANG}}` placeholder sets the code fence language (e.g., `js`, `swift`, `kotlin`, `dart`, `csharp`, `python`, `cpp`, `rust`)
  - Configuration and Platform Notes sections are free-form text — adapt to each SDK's specific needs
---

## 2026-02-22 - US-006
- Created `llms.txt` at root of `aptabase-electron` repo
- Documents main vs renderer process split with separate import paths
- Covers: initialization (main process), trackEvent (both processes), properties, configuration (self-hosted host option)
- Platform notes: Electron >= 3.x, must init before app.whenReady(), event buffering, IPC relay
- File size: 3,014 bytes
- Files changed: `aptabase-electron/llms.txt` (new file)
- Committed on branch `ralph/llms-txt-agent-discovery` in aptabase-electron repo
- **Learnings for future iterations:**
  - SDK repos need their own feature branch created (they're separate git repos, not the same as aptabase or aptabase-com)
  - `initialize` function in Electron SDK has an `AptabaseOptions` type with optional `host` for self-hosted instances
  - Electron SDK uses IPC and a custom `aptabase-ipc` protocol to relay events from renderer to main process
  - Sessions auto-rotate after 1 hour of inactivity (SESSION_TIMEOUT = 3600s)
  - The template is a good starting point but Electron needs a custom "Main vs Renderer Process" section not in the template
---
